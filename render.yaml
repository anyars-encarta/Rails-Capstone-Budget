databases:
  - name: e-budget
    databaseName: e-budget_qh3v
    user: e-budget_qh3v_user

services:
  - type: web
    name: e-budget
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: anyarsyussif
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false